# Crawler MCP server (Python) — core dependencies
mcp>=1.2.0            # Model Context Protocol SDK (FastMCP + transports)
crawl4ai>=0.5.0      # Headless-browser crawling, fit-markdown, structured extraction
httpx>=0.27.0        # Fast static (no-browser) fetch path
beautifulsoup4>=4.12 # HTML parsing for selector/link/table helpers
lxml>=5.0            # Fast parser backend for BeautifulSoup
python-dotenv>=1.0   # Load configuration from .env
